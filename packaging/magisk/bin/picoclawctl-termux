#!/system/bin/sh
CTL=/data/adb/modules/picoclaw/bin/picoclawctl
quote_arg() { printf "'%s'" "$(printf "%s" "$1" | sed "s/'/'\\''/g")"; }
cmd="$CTL"
for arg in "$@"; do cmd="$cmd $(quote_arg "$arg")"; done
exec su -c "$cmd"
